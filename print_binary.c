#include "main.h"
#include "main.h"
/**
 * print_binary - print binary information
 * @arg: a pointer to a binary
 * Return: a pointer to
*/
#include "main.h"
/**
 * print_binary - print binary information
 * @arg: a pointer to a binary
 * Return: a pointer to
*/
int print_binary(va_list arg)
{
	unsigned int n;
	char arr[100];
	int num = 0;
